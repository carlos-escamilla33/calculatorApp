class Calculator {
    constructor() {
        this.color = color;
    }

    calcAdd(num1, num2) {
        return num1 + num2;
    }

    calcSub(num1, num2) {
        return num1 - num2;
    }

    calcDiv(num1, num2) {
        return num1 / num2;
    }

    calcMult(num1, num2) {
        return num1 * num2;
    }

    createButtons() {
        const calcBody = document.querySelector("#calculator");
    }
}